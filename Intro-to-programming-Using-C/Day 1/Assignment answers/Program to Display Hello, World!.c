#include <stdio.h>
#include <stdlib.h>

int main() {
    // Display "Hello, World!" on the screen
    // Use the printf function to print a string
    // Use \n to add a newline character at the end of the string
     printf("Hello, World!\n");
    // Return 0 to indicate the successful termination of the program
    return 0;
}

/*
This is a simple C program that prints "Hello, World!" on the screen
It uses the stdio.h header file to access the input/output functions
It uses the stdlib.h header file to access the memory allocation functions
*/
